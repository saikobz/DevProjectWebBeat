import { Card } from "@/components/ui/card";

export default function NewBeatPage() {
  return (
    <Card>
      <h1 className="text-3xl font-black text-white">Upload New Beat</h1>
      <p className="mt-2 text-zinc-400">MVP form placeholder: รอบแรกใช้ mock admin manager ก่อนต่อ multipart upload + FFmpeg + R2</p>
    </Card>
  );
}
